# 4.4.5 — 2026-08-27 — очистка повторяющихся полей опросника

- Убраны 9 дублирующих ручных полей, уже известных из выбранного исполнения.
- Добавлен блок «Параметры выбранного исполнения» перед расширенным опросником.
- В summary показываются производитель, модель/исполнение, ID, тип, размещение, форма, подвеска, грузоподъёмность, скорость и диаметр каната.
- Повторный серийный/идентификационный номер в чертёжной части убран.
- Количество пользовательских инженерных полей сокращено с 67 до 58.
- Исторические заявки и черновики остаются совместимыми: старые ключи не удаляются из БД.
- Полная документация обновлена до `LIFTORG_PROJECT_DOCUMENTATION_v4.4.5_FULL.md`.

# 4.4.4 — 2026-08-27 — терминология отводного блока

- `Тип шкива` переименован в `Тип отводного блока / схема огибания канатом`.
- A/B/C переведены на русский: <90°, 90°, 180°.
- Добавлена подсказка с привязкой к оригинальному эскизу Torin.
- Внутренний код поля сохранён для обратной совместимости черновиков и заявок.
- Полная документация обновлена до v4.4.4.

# 4.4.4 — 2026-08-26 — подтверждённая классификация размещения

- По уточнению инженера реализовано правило MR/MRL.
- Все безредукторные бочонки классифицируются как `Без машинного помещения`.
- Плоские таблетки `WE 06`, `WE 10`, `WE 20`, `WJC-*-T` классифицируются как `Без машинного помещения`.
- Остальные безредукторные таблетки классифицируются как `В машинном помещении`.
- Добавлена startup-миграция старой SQLite-БД; ручные значения `placement_type` не перезаписываются.
- В payload сохраняется `placement_type_source`.
- По текущим 838 исполнениям: MRL=498, MR=334, UNKNOWN=6, coverage=832/838.
- Добавлены тесты конкретных серий и противоположного фильтра.
- FULL-документация обновлена до `LIFTORG_PROJECT_DOCUMENTATION_v4.4.4_FULL.md`.


## 4.4.0 — TDNA translated survey
- Получен реальный русский перевод `для воплощения на сайте-1.docx`.
- Полностью зачёркнутые поля исключены.
- Старые 142 Excel-поля в клиентской форме заменены точной компактной TDNA-структурой из 66 полей / 6 групп.
- Подключены оригинальные инженерные эскизы из загруженного `TDNA Traction Survey.pdf`.
- Автоподстановка, автосохранение, черновики, RFQ и коммерческий блок сохранены.
- Документация обновлена параллельно.

# v4.1.0 test-admin

- В тестовом окружении отключена HTTP Basic авторизация для `/admin` и `/api/admin/*`.
- Добавлен прямой URL редактирования `/admin/product/{id}?id={id}`.
- В списке админки добавлены ссылки «Карточка» на `/product/{id}`.
- Перед production авторизацию необходимо включить обратно.

# CHANGELOG

## 4.1.0 — 25.08.2026
- Зафиксирован единый full-platform baseline.
- Исправлен главный дефект фильтрации: пустые исходные значения больше не считаются несовпадением.
- Добавлен explainable matching PASS/UNKNOWN/FAIL.
- Добавлены exact/clarification totals и объяснение на карточках.
- В React добавлен коммерческий калькулятор по выбранной модели.
- Отпускная цена производителя подставляется в расчёт автоматически при наличии.
- Полный raw Excel доступен в технической карточке.
- Добавлен конструктор динамических характеристик `/admin/attributes`.
- Добавлены таблицы `attributes` и `product_attribute_values`.
- Поддержано отображение реального `image_ref`, когда фото будет привязано.
- Обновлена полная проектная документация.


## 4.1.0 — UX polish
- Упрощена навигация и добавлена прямая ссылка «Управление».
- Фильтры получили явное состояние «изменены / применены», sticky CTA и быстрый сброс.
- Результаты объясняют мягкую инженерную логику: точно / требует уточнения.
- Карточки компактнее: действия разложены 2×2, калькулятор и запрос цены доступны без перегруза.
- Подсветка моделей, требующих уточнения, стала заметной, но не тревожной.
- Улучшены focus/hover/mobile состояния.


## 4.2.0 — Catalog UX / модель и исполнение
- Каталог группирует одинаковые `manufacturer + model` в одну карточку модели.
- 838 строк исходной БД не удаляются: инженерный подбор по-прежнему выполняется по каждому исполнению.
- В API поиска добавлен режим `group_models=true` (по умолчанию).
- Ответ поиска содержит количество моделей и количество подходящих исполнений.
- Карточка модели показывает количество исполнений и диапазоны ключевых характеристик.
- Детальная карточка получила таблицу исполнений и явный выбор конкретного исполнения.
- Сравнение, калькулятор и запрос цены выполняются только для выбранного исполнения.
- Увеличена рабочая область фотографии; `object-fit: contain`, единая высота медиаблока.
- Сохранена объяснимая логика PASS / UNKNOWN / FAIL.

## 4.4.0 — 2026-08-26 — Survey UX / Torin sketches / autosave

- Переработан опросный лист в двухуровневый RFQ: короткий лид + расширенная инженерная часть.
- Добавлены оригинальные SVG-эскизы по логике TDNA Traction Survey: размещение MR/MRL, rope drop X1/X2/X3, deflector Ddef/Ys, габариты/Z1/Z2.
- Поля опросника теперь расположены сеткой, секции сворачиваемые, неизвестные значения разрешено пропускать.
- Добавлено локальное и серверное автосохранение черновика.
- Добавлена таблица SQLite `inquiry_drafts` и API GET/PUT/DELETE `/api/inquiry-drafts/{token}`.
- Токен черновика сохраняется в URL, поэтому обновление страницы не сбрасывает форму.
- Добавлена кнопка «Сохранить и продолжить позже».
- Сохранён блок «Коммерческий запрос и поставка» с формулировкой заказчика от 26.08.2026.
- Существующие 838 исполнений, группировка моделей, подбор PASS/UNKNOWN/FAIL, карточки, сравнение, калькулятор и админка не изменены.

## 4.4.1 — 2026-08-26 — отдельный заказ без обязательного опросника
- По замечанию инженера/заказчика опросный лист перестал быть обязательным путём конверсии.
- В карточке модели и исполнения добавлены два независимых действия: **«Заполнить опросный лист»** и **«Заказать лебёдку»**.
- `/product/{id}/inquiry` — расширенный инженерный TDNA-опросник с эскизами и автосохранением.
- `/product/{id}/order` — короткий коммерческий запрос без технической «простыни».
- В коротком заказе сохранён блок «Коммерческий запрос и поставка» и текст: «Запрос на индивидуальное ценовое предложение в зависимости от количества партии лебёдок и сроков производства».
- Прямой заказ сохраняется в общей таблице `inquiries` с `request_kind=direct_order`.

## Documentation consolidation — 26.08.2026
- Добавлен единый FULL-документ `LIFTORG_PROJECT_DOCUMENTATION_v4.4.1_FULL.md`.
- Сведены каталог, model/execution UX, PASS/UNKNOWN/FAIL, изображения, калькулятор, новый CPQ EXW→DDP, RFQ, TDNA survey, direct order, CRM/BPMSoft scope и future lift catalog.
- Явно разделены статусы «реализовано» и «принято в требования / требует реализации».
- Зафиксированы новые комментарии инженера по длине 8/10/12 м и плоским tablet-исполнениям как pending clarification.


## Подтверждённые замечания инженера — 27.08.2026 (v4.4.4)

В инженерном опроснике обязательны следующие правила:

1. Пользовательские подписи и подсказки должны быть однозначными и инженерно понятными; рядом со сложными параметрами приводится пример или пояснение, что именно измерять/выбирать.
2. Пользовательский интерфейс использует систему СИ. Футы, фунты и дюймы из оригинального американского TDNA Survey не используются как единицы ввода. Геометрические размеры задаются в мм, длины — в м, масса — в кг, ускорение — в м/с².
3. Поле 208–230 / 460–480 называется «Выходное напряжение привода, В», а не «мощность».
4. Для параметров, завязанных на эскизы Torin, интерфейс явно показывает ссылку «См. рис. N».
5. Каждый эскиз в опроснике увеличивается по клику в полноэкранном lightbox; закрытие — кнопкой, кликом по фону или Esc.
6. Неоднозначное поле «Расположение существующей закладной балки под лебёдку» заменено понятным выбором схем A–E из Torin Illustration 3 с пояснением.
7. Трос Боудена называется «Устройство дистанционного растормаживания». Он находится в инженерном опроснике рядом с длиной кабеля энкодера, а не в калькуляторе. Значения: «Не требуется», «8 м (стандарт)», «10 м», «12 м». Правило действует как для машинного помещения, так и для MRL.
8. Опросник остаётся необязательным: пользователь может сразу выбрать «Заказать лебёдку» и отправить короткий коммерческий запрос.


## v4.4.6 — медиабиблиотека оборудования

Реализована управляемая медиабиблиотека без привязки к правке кода.

Правила наследования медиа:

1. `execution` — конкретное исполнение имеет наивысший приоритет;
2. `model` — если у исполнения нет своего фото, используются материалы модели;
3. `family` — если у модели нет собственного материала, используется семейство производителя;
4. старое `image_ref` остаётся безопасным fallback для обратной совместимости.

Одна модель/семейство/исполнение может иметь несколько материалов. Поддерживаемые типы: `photo`, `drawing`, `document`. Для фото можно назначить основное изображение и порядок сортировки. Основное фото автоматически используется в каталоге, карточке и сравнении.

Добавлена таблица SQLite `media_assets` со связями `manufacturer / family / model / product_id`, типом материала, URL, названием, признаком основного фото и сортировкой. При первом запуске существующие изображения из `image_ref` и сохранённые семейные файлы `/static/winches` автоматически регистрируются в медиабиблиотеке.

Администратор получает страницу `/admin/media`, где можно загрузить JPG/JPEG/PNG/WEBP/GIF/SVG/PDF, выбрать уровень привязки, тип материала, основное фото и порядок. В карточке позиции из `/admin` добавлена ссылка `Медиа`.

Публичный API `/api/products/{id}` теперь отдаёт `media`, `media_family`, `primary_image`; `/api/products/{id}/media` возвращает разрешённую галерею. Карточка оборудования показывает галерею `Фото / Чертежи / Документы`.

Важно для обновлений: вместе с БД необходимо сохранять оба каталога `/app/static/winches` и `/app/static/media-library`. Загруженные файлы не удаляются физически при удалении привязки из БД, чтобы не разрушать исторические snapshots заявок.


## v4.4.7 — разграничение видимости калькулятора

- В режиме «Покупатель» скрыты закупочная цена, стоимость доставки, срок производства, срок доставки и внутренние коэффициенты.
- Покупателю показываются только итоговые цены по 4 сценариям оплаты.
- В режиме «Сотрудник» сохранён полный набор внутренних параметров и расшифровка расчёта.
- Базовые формулы `Калькулятор-2.xlsx` не изменялись.
- Требование зафиксировано по обратной связи заказчика 27.08.2026.


## v4.4.8 — Persistent Media / media outside Docker image

- Каталог `media-library` вынесен в bind volume `./media-library:/app/app/static/media-library`.
- Каталог legacy-фото `winches` вынесен в bind volume `./winches:/app/app/static/winches`.
- Пользовательские фото, официальные PDF, CAD/ZIP и чертежи больше не запекаются в Docker image.
- Добавлен `.dockerignore`, исключающий `app/static/media-library/**` и `app/static/winches/**` из build context.
- Обновление приложения больше не должно удалять или повторно копировать медиабиблиотеку.
- Перед обновлением с v4.4.7 содержимое `app/static/media-library` и `app/static/winches` необходимо один раз перенести в новые корневые persistent-каталоги `media-library/` и `winches/`.
- SQLite остаётся persistent через `./storage:/app/storage`.
- Цель: Docker build содержит только код/статические UI-ресурсы, а тяжёлые и изменяемые медиа живут на хосте.


## v4.4.9 — Explicit Execution Selector / различия исполнений

Подтверждённое UX-замечание заказчика: когда одна модель содержит несколько исполнений, пользователь должен однозначно понимать, как переключать исполнения и чем они отличаются.

Реализовано:
- вся строка исполнения кликабельна;
- добавлен явный radio-индикатор выбора и бейдж «Выбрано»;
- выбранная строка визуально подсвечивается;
- текст прямо сообщает, что выбранное исполнение используется для расчёта, опросного листа и заказа;
- таблица автоматически показывает только те нормализованные технические характеристики, значения которых реально различаются между исполнениями данной модели;
- различающиеся ячейки визуально выделяются;
- управление доступно мышью и клавиатурой Enter/Space;
- при переключении заново загружается полный product record конкретного `product_id`, поэтому медиатека, технический паспорт, сравнение, калькулятор, опросник и заказ работают с выбранным исполнением.

Контрольный пример из текущей БД: модель `WE 06-630-100(400)` содержит исполнения `#360` и `#395`. Их видимые базовые параметры совпадают, но масса различается: `280 кг` против `310 кг`. В v4.4.9 таблица автоматически показывает именно столбец «Масса», не заставляя пользователя искать отличие вручную.


## v4.4.9.1 — frontend boot hotfix
- Исправлена совместимость JS блока выбора исполнения.
- Убран потенциально проблемный новый синтаксис из runtime-критичного участка.
- Cache-version React assets изменён на `4491`, чтобы браузер не использовал старый app.js.
- БД, media-library и winches не изменяются.


## v4.4.9.2 — runtime fix execution selector
- Переписан селектор исполнений без динамического React.createElement.apply/IIFE.
- Добавлена фильтрация повреждённых/null execution records.
- Добавлен Error Boundary: ошибка одной UI-ветки больше не должна давать полностью белый экран.
- Cache-busting React assets: v=4492.
- БД, калькулятор и persistent media не изменяются.


## v4.4.10 — WJC-T: объёмная таблетка + MRL — 28.08.2026

По прямому уточнению заказчика серия **WJC-T** классифицируется как **«Безредукторная объёмная таблетка»** и имеет тип установки **«Без машинного помещения» (MRL)**.

Реализовано:
- все 31 исполнения WJC с суффиксом `-T` переводятся в тип `Безредукторная объёмная таблетка`;
- для них принудительно сохраняется `placement_type = Без машинного помещения`;
- исправлен источник классификации: `Правило 28.08.2026: WJC-T = объёмная таблетка, MRL`;
- старая ошибочная пометка WJC-T как «плоская таблетка» больше не используется;
- новый тип добавлен в фильтр, API/meta и админский список типа лебёдки;
- логика односкоростной безредукторной машины учитывает новый тип;
- startup-миграция исправляет существующую рабочую SQLite-БД при первом запуске v4.4.10, поэтому ручной SQL не требуется;
- прочие технические параметры WJC-T не изменяются.

Контрольные проверки:
- WJC-T: 31 исполнение;
- тип: `Безредукторная объёмная таблетка`;
- размещение: `Без машинного помещения`;
- совместный фильтр по этому типу + MRL возвращает 31 исполнение.
